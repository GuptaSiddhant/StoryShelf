node_modules
out

.turbo
.cache
build
esm
cjs
public/build
.env
.env.*
*.tsbuildinfo
coverage
dist

.DS_Store
vite.config.ts.timestamp*

.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

*storybook.log
.npmrc

storybook-static
fixtures/**/node_modules
fixtures/**/storybook-static
fixtures/**/dist
fixtures/**/pnpm-workspace.yaml
.test-results
test-report
coverage
openapi.json

# turbo prune emits the pruned pnpm-format lock as pnpm-lock.yaml (renamed to
# nub.lock in Docker); never commit a root-level copy (nub.lock is source of truth)
pnpm-lock.yaml
!fixtures/**/pnpm-lock.yaml

# StoryShelf local data
.data/
.dev-data/
data
nub.lock.bak
.astro

# scratch space for agents (prefer over /tmp)
.tmp

# opencode state (keep skills versioned)
.opencode/**
!.opencode/skills/
!.opencode/skills/**
.ocx/