/**
 * Database adapter entrypoint.
 *
 * Canonical implementations live in `db/`; re-exported here so existing
 * `@storyshelf/core/adapter/database` imports keep working.
 */
export * from "../db/database.ts";
